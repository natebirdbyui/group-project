import { showSlide, nextSlide, startAllSlideshows, stopAllSlideshows } from "./slideshow.js";

// Updates the year in the footer
document.getElementById("year").textContent = new Date().getFullYear();

